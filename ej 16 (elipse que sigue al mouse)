float x,y,velx,vely;
float angulo,distancia;
void setup(){
  
 size(800,800); 
background(0);

x=width/2;
y=height/2;
}


void draw(){
  int tam=100;
  float dx= mouseX - x-tam/2;
  float dy= mouseY - y-tam/2;

 velx=dx/50;
 vely=dy/50;
 x=x+velx+1;
 y=y+vely+1;
 ellipse(x,y,tam,tam);
}
