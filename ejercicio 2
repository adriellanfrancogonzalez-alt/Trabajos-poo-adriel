void setup(){
 size(400,400);
 
   
}

void draw(){
 float w=width, h=height;
 background(100,200,300);
     //tamaño cuadrado
  circle(w/2,h/2-w/4,w/4); //cuadrado responsive
 
                 //tamañno circulo
circle(w/2,h/2,w/4); //circulo responsive

fill(255,0,0);

 
}
 
}
